version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: renatanerenata/devops-for-programmers-project-lvl1
    volumes:
      - "/app"
    command: npm test
